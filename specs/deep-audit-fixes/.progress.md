## Reality Check (BEFORE)

**Goal type**: Fix
**Reproduction command**: `python3 -m pytest plugins/ralph-parallel/scripts/ -q && bash plugins/ralph-parallel/hooks/scripts/test_gate.sh && bash plugins/ralph-parallel/hooks/scripts/test_teammate_idle_gate.sh && bash plugins/ralph-parallel/scripts/test_session_isolation.sh && bash plugins/ralph-parallel/scripts/test_stop_hook.sh`
**Exit code**: 0 (all passing)
**Test counts**: 155 pytest + 10 gate + 7 idle + 43 session + 113 stop = 328 total

**Key issues to resolve**:
- C1: `_sanitize_cmd()` allows `;`, `&&`, `||`, `|` command chains through eval
- C2: TASK_ID used unescaped in grep -E / sed regex patterns
- C3: SESSION_ID not validated before use in file paths
- H1: Pipeline failure in session-setup.sh orphaned team cleanup
- H2: git rev-list --count on orphaned SHAs fails silently
- H3: printf '%b' interprets escape sequences from task descriptions
- H4: Here-strings `<<<`, bash arrays, `IFS read -ra` (3 files)
- H5: Race conditions in counter files (document-only)
- M1-M10: Various medium issues across shell and Python files

## Completed Tasks
- [x] 1.1 Harden _sanitize_cmd() in task-completed-gate.sh (C1a)
- [x] 1.2 Harden _sanitize_cmd() in capture-baseline.sh (C1b)

## Current Task
Awaiting next task

## Learnings
- All 328 tests currently pass across 5 test suites
- `_sanitize_cmd()` is duplicated in task-completed-gate.sh and capture-baseline.sh
- `_sanitize_name()` is duplicated in 4 files (session-setup, dispatch-coordinator, teammate-idle-gate, merge-guard) -- already validates `^[a-zA-Z0-9][a-zA-Z0-9_-]*$`
- `.tmp.$$` pattern used in session-setup.sh (5 places) and dispatch-coordinator.sh (1 place) -- PID-predictable temp files
- `<<<` here-strings used in 3 files: file-ownership-guard.sh:89, teammate-idle-gate.sh:137, task-completed-gate.sh:250
- No Python file uses `from __future__ import annotations` -- but Python 3.11 in use, so `dict | None` syntax already works natively
- `BASH_SOURCE[0]` in 3 files: test_stop_hook.sh:26, test_session_isolation.sh:27, merge-guard.sh:73
- merge-guard.sh already has `$0` fallback for BASH_SOURCE
- `timeout` command used once (session-setup.sh:68) with no fallback check
- `.[0].path` marketplace query in session-setup.sh:59
- _sanitize_cmd regex pattern `';|\||\&\&|\|\|'` works correctly: rejects ;, |, &&, || but allows --, =, clean commands
- Adding checks to _sanitize_cmd shifts line numbers; verify commands using sed line ranges need updating (function now ends at line 40, was 35)
- sed range `/^_sanitize_cmd/,/^}/p` matches too much in gate file because line 138 starts with `_sanitize_cmd` (function call); Python extraction confirms both functions are identical
