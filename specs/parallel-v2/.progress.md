# Progress: parallel-v2

## Reality Check (BEFORE)

**Goal type**: Fix (11 improvements to existing plugin based on demo run lessons)
**Reproduction**: Manual review of plugin files — issues are structural/behavioral, not runtime errors
**Key issues to resolve**:
- Issue 1: task-completed-gate.sh uses fragile regex to extract spec task ID from group-level TaskList tasks (lines 84-100)
- Issue 2: dispatch-state.json `completedGroups` never updated during execution (dispatch.md Step 6 writes `[]`, Step 8 never updates)
- Issue 3: No timeout/stall handling in lead coordination loop (dispatch.md Step 8)
- Issue 4: verifyTasks is flat array `["1.8", "2.4"]` with no phase metadata
- Issue 5: status.md checks git log for progress instead of TaskList
- Issue 6: Confusing merge flow docs — file-ownership doesn't need /merge but docs imply it does
- Issue 7: No abort mechanism for dispatch
- Issue 8: Partition rebalancing algorithm undefined (Step 4 mentions but doesn't define)
- Issue 9: No real-time file ownership enforcement (only post-hoc in merge)
- Issue 10: No --watch mode for status
- Issue 11: No end-to-end worked example

## Learnings
- Plugin is 10 files (~1073 lines), all markdown instructions and bash scripts
- Commands are markdown instruction files (dispatch.md, status.md, merge.md) that tell Claude what to do
- Hooks are bash scripts (task-completed-gate.sh, session-setup.sh) registered in hooks.json
- dispatch.md is the largest file (~413 lines) and most complex — most changes target it
- task-completed-gate.sh has the fragile regex logic on lines 84-100 that tries to extract spec task IDs from TaskList task subjects
- dispatch-state.json schema has `completedGroups: []` but no code updates it
- team-prompt.md and teammate-prompt.md are reference docs, not templates used directly
- Synthesis: dispatch.md receives the most edits (7 of 11 improvements touch it) — Steps 4, 6, 7, 8 all change
- Synthesis: The 1:1 TaskList mapping (P0-1) is the keystone change — simplifies hook, enables per-task status, improves teammate prompt
- Synthesis: verifyTasks format change (P0-4) must be backward-compatible since existing dispatch-state files use flat arrays
- Synthesis: session-setup.sh needs no changes — it's already well-designed for dispatch lifecycle
- Synthesis: hooks.json doesn't need structural changes — PreToolUse guidance goes in dispatch.md as documentation only
- Synthesis: 20 total tasks across 4 phases — 13 POC, 2 refactoring, 2 testing, 2 quality gates
