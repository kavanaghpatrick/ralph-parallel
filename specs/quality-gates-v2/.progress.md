# Progress: quality-gates-v2

## Original Goal
Fix 3 audit issues from v6 dispatch — (1) phase gate clippy enforcement not blocking, (2) tasks.md completion marks not written back, (3) worktree commit provenance unclear. Prioritize deterministic script logic over prompt/prose changes. All fixes must be ecosystem-agnostic.

## Status
- Phase: Execution
- Started: 2026-02-22

## Completed Tasks
- [x] 1.1 Add Stage 6 lint check to task-completed-gate.sh
- [x] 1.2 Create mark-tasks-complete.py script
- [x] 1.3 Add commit provenance convention to build-teammate-prompt.py
- [x] 2.1 Create verify-commit-provenance.py script
- [x] 2.2 Harden mark-tasks-complete.py edge cases
- [x] 2.3 Add error handling to lint stage in task-completed-gate.sh
- [x] 3.1 Test mark-tasks-complete.py with synthetic data
- [x] 3.2 Test verify-commit-provenance.py with synthetic git log

## Reality Check (BEFORE)

**Goal type**: Fix
**Reproduction command**: `grep -c 'lint' ralph-parallel/hooks/scripts/task-completed-gate.sh`
**Exit code**: 1 (no matches)
**Error output**:
```
No matches found -- "lint" does not appear anywhere in task-completed-gate.sh
```

**Key errors to resolve**:
- Error 1: task-completed-gate.sh has 0 references to lint. Stages 1-5 cover verify, typecheck, file existence, build, test -- but NOT lint. qualityCommands.lint is never read.
- Error 2: No script exists to write `- [x]` marks back to tasks.md. completedGroups in dispatch-state.json tracks completion but nothing maps it to checkbox updates.
- Error 3: No commit provenance mechanism. build-teammate-prompt.py generates prompts with no commit attribution convention. No Signed-off-by, no agent tags, no trailers.

## Current Task
Awaiting next task

## Next
Task 4.1 Validate all scripts and sync to plugin cache

## Learnings
- Stage 6 lint follows exact same structure as Stage 4 build: jq read, interval check, eval, exit 2 on failure. LINT_INTERVAL defaults to 3 (same as BUILD_INTERVAL).
- task-completed-gate.sh Stage 4 (periodic build) is the exact template for Stage 6 (periodic lint) -- same pattern: read cmd from dispatch-state via jq, check interval, run, block on failure
- dispatch-state.json completedGroups contains group names (strings), not task IDs. Must join with groups[].tasks to get task IDs for writeback.
- build-teammate-prompt.py already has a `name` variable in scope at the point where commit convention would be injected -- no new data needed
- The existing quality-gates v1 spec (ralph-parallel/specs/quality-gates/) already implemented baseline capture and verify command validation. This v2 addresses the gaps it missed.
- parse-and-partition.py's partition output includes both group `name` and `tasks` (list of IDs) per group -- mark-tasks-complete.py can rely on this structure in dispatch-state.json
- mark-tasks-complete.py: regex uses re.escape on task IDs and word boundary \b to avoid partial matches (e.g., "1.1" not matching "1.10")
- verify-commit-provenance.py: uses %x00 (null byte) as record separator in git log format to cleanly split multi-line trailer output per commit. Always exits 0 with JSON output even on errors (audit tool pattern).
- mark-tasks-complete.py hardening: tasks.md not found now exits 1 (was 0), --dry-run flag skips file write but still reports counts, os.path.isfile check before open avoids race conditions
- Stage 6 lint hardening: added "null" guard for LINT_CMD (matching BASELINE_COUNT guard in Stage 5), enhanced failure message with interval info, enhanced success message with task count
- test_mark_tasks_complete.py: 5 test classes using pytest tmp_path fixture. Runs script as subprocess for true integration testing (no imports/mocking of internal functions).
- test_verify_commit_provenance.py: 3 main test classes + edge cases. Uses importlib to load hyphenated module and unittest.mock.patch to mock subprocess.run. Synthetic git log output uses %x00 null byte separator matching real git format.
