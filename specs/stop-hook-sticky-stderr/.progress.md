# Progress: stop-hook-sticky-stderr

## Original Goal
Fix the stop hook "sticky stderr" bug where dispatch-coordinator.sh exit-2 messages persist in conversation context after the blocking condition is resolved, causing confusion. The stop hook correctly allows stop on retry (stop_hook_active=true → exit 0), but the original stderr coordination prompt is already baked into the conversation and can't be un-injected. Need a comprehensive fix across the hook harness.

## Interview Format
- Version: 1.0

## Intent Classification
- Type: REFACTOR
- Confidence: high (3 keywords matched)
- Min questions: 3
- Max questions: 5
- Keywords matched: fix, improve, clean up

## Interview Responses

### Goal Interview (from start.md)
- Problem: Both - stale stderr messages confuse the model AND incorrect blocking in certain scenarios
- Constraints: No constraints - comprehensive overhaul OK. Can rethink the stop hook approach entirely.
- Success criteria: Clean stop behavior after status changes + correct handling of all dispatch lifecycle states + concurrent session safety (no cross-session interference)

## Completed Tasks
- [x] 1.1 Rewrite dispatch-coordinator.sh with JSON decision control and block counter
- [x] 1.2 Update session-setup.sh with heartbeat-gated auto-reclaim
- [x] 1.4 Create test_stop_hook.sh test suite
- [x] 2.1 Update test_session_isolation.sh for new exit code behavior
- [x] 2.2 Add error handling hardening to dispatch-coordinator.sh

## Current Task
Awaiting next task

## Learnings
- Claude Code conversation is append-only: hook stderr messages injected via exit 2 persist permanently in conversation history. No API to retract/expire them.
- Two signaling approaches for Stop hooks: exit 2 (stderr-only, legacy) vs exit 0 with JSON `{"decision":"block","reason":"..."}` (stdout, recommended). Cannot mix -- exit 2 ignores stdout JSON.
- Plugin bug #10412 (CLOSED): stop hooks with exit 2 fail when installed via plugin system, showing "Stop hook prevented continuation" instead of continuing. Fix status unclear. JSON approach is more reliable for plugins.
- UX issue #12667 (OPEN): JSON approach triggers misleading "Stop hook error:" label in UI. Cosmetic, not functional.
- `stop_hook_active` field: when true, Claude is already continuing from a previous stop hook block. Current code unconditionally exits 0, giving coordinator exactly ONE turn before escape. Official docs say "check this value or process the transcript to prevent running indefinitely."
- Stop hook input schema: `session_id`, `stop_hook_active`, `last_assistant_message`, `transcript_path`, `cwd`, `permission_mode`, `hook_event_name`.
- Auto-reclaim in session-setup.sh can steal dispatches from concurrent sessions. Guard (`TEAM_EXISTS`) is insufficient -- doesn't distinguish "same user restarted" from "different user in parallel."
- jq tmp+mv write pattern is atomic at filesystem level but read-modify-write cycle is not. Low practical risk since Stop hook only reads.
- The sticky stderr problem is unique to Stop hooks. PreToolUse exit 2 blocks a single tool call (transient). TaskCompleted exit 2 prevents task completion (retryable). Stop exit 2 injects a persistent conversation message.
- All 4 hooks in ralph-parallel use exit 2 + stderr for blocking. None use JSON decision control. Migration to JSON is the recommended fix for the Stop hook specifically.
- Hook timeout is 10s (per hooks.json). jq on small JSON takes ~10ms. Scanning 100+ specs is safe within timeout.
- No dedicated test harness for hook scripts. test_session_isolation.sh (from session-isolation spec) is the closest precedent for bash hook testing.
- Quality commands: tsc (build), tsc --noEmit (typecheck), node ./scripts/test.mjs (test). No lint command exists for the project.
- Requirements phase: Whether JSON `reason` persists in conversation history like stderr is UNRESOLVED -- needs empirical testing. This is the single biggest risk to the core fix. If reason persists identically, the fix reduces severity (shorter, state-aware messages) but doesn't eliminate stickiness.
- Requirements phase: Block counter approach (temp file per dispatch+session) is preferred over env var approach because CLAUDE_ENV_FILE is broken on resume (#24775). Temp files survive session restarts.
- Requirements phase: The "all tasks complete" stderr reminder (line 134 of dispatch-coordinator.sh) should be removed entirely -- it contributes to sticky context and the coordinator already has instructions in dispatch.md.
- Requirements phase: Heartbeat approach for cross-session safety is preferred over process-existence checks (platform-specific, fragile) or user-explicit --reclaim (poor UX). 10-minute threshold balances safety vs usability.
- Requirements phase: PreToolUse and TaskCompleted hooks should NOT be migrated to JSON -- their exit-2 usage is transient/retryable and doesn't cause the sticky problem. Only the Stop hook needs migration.
- Requirements phase: Process-unique tmp filenames (file.tmp.$$) prevent a subtle collision when session-setup.sh and dispatch.md write to the same state file near-simultaneously. Current code uses bare `.tmp` suffix.
- Design phase: Block counter format uses `count:status:dispatchedAt` triple to auto-detect dispatch transitions (abort/re-dispatch) without needing explicit cleanup coordination. Simpler than a separate reset mechanism.
- Design phase: Heartbeat write triggers ONLY on block (not every invocation). Allow-exits (terminal status, safety valve) skip heartbeat. This means "heartbeat present" reliably indicates "coordinator was actively blocked" rather than "hook ran at all."
- Design phase: JSON reason messages designed as short pointers (~150-250 chars) referencing current state, not full coordination prompts (~500-800 chars). Even if JSON reason persists in conversation like stderr did, shorter messages degrade more gracefully under compaction.
- Design phase: `block_stop()` should use `jq -nc --arg r "$REASON" '{"decision":"block","reason":$r}'` instead of `printf` to guarantee valid JSON even with unusual spec names containing quotes or backslashes.
- Design phase: macOS date compatibility requires `date -j -f "%Y-%m-%dT%H:%M:%SZ"` (BSD) with fallback to `date -d` (GNU). Both produce epoch seconds for heartbeat age calculation.
- Design phase: The "all tasks complete" stderr reminder (line 134) is removed entirely -- it was the most commonly sticky message since it fires at end-of-dispatch when the model is already trying to stop. Coordinator has dispatch.md instructions for cleanup.
- Design phase: Safety valve (MAX_BLOCKS exit) does NOT delete the counter file. This prevents a loop where: reach MAX_BLOCKS -> allow -> restart -> counter gone -> block again -> reach MAX_BLOCKS -> etc. Counter persists until dispatch transitions to terminal status.
- Error hardening: Every error path follows a "safe default" principle: write failures -> block still works (just without tracking); read failures -> allow stop (can't verify dispatch is active); date failures -> epoch 0 = "very stale" = allow reclaim. The pattern is: when in doubt, prefer allowing user to stop/reclaim over trapping them.
- Task planning: Two files change (dispatch-coordinator.sh rewrite, session-setup.sh modify), zero new files except test_stop_hook.sh. Total ~15 tasks across 5 phases.
- Task planning: Existing test_session_isolation.sh (610 lines) expects `exit 2` for blocking -- must be updated in Phase 2 to expect `exit 0` + JSON stdout. 8 tests need exit code assertion changes.
- Task planning: shellcheck is NOT available on this machine (`which shellcheck` returns not found). Syntax validation uses `bash -n` only. Recommend installing shellcheck for future CI.
- Task planning: The test_session_isolation.sh pattern (setup helpers, begin_test/end_test, assert_* functions, grep-based verification) is the canonical test pattern for this project. New test_stop_hook.sh follows this pattern exactly.
- Task planning: Key risk -- tasks 1.1 and 1.2 are the only implementation tasks and they touch different files, making them parallelizable by ralph-parallel dispatch. Task 1.4 (test creation) depends on both being complete to verify behavior.
- Task planning: No "Reality Check (BEFORE)" section in .progress.md, so no VF task needed. This is a REFACTOR-type spec, not a fix-and-verify.
- Test 1.4: Stop hook tests must run from within tmpdir (cd $tmpdir) so git rev-parse fails and falls back to CWD from stdin JSON. Without this, the hook resolves PROJECT_ROOT to the real git repo and scans real spec dirs. Tests using TEAM_NAME mode (CLAUDE_CODE_TEAM_NAME env var) bypass this because the hook derives spec name from team name. Counter file cleanup only fires in team-name mode (not scan mode), because scan mode skips non-dispatched states.
- Test 1.4: 12 tests, 48 assertions covering all 6 design scenarios, 4 ported tests with updated expectations (exit 0 instead of exit 2), and 2 backward compat tests. All pass.
- Test 2.1: Session isolation tests run_stop_hook() must cd into tmpdir (like test_stop_hook.sh) so git rev-parse fails and falls back to CWD from stdin JSON. Without this, the hook resolves to the real git repo and scans real spec dirs, causing false test behavior. Added $tmpdir parameter to all run_stop_hook calls.
- Test 2.1: Added assert_stdout_contains_json_block() helper and cleanup_block_counters() to begin_test(). Updated 10 exit-code assertions (T-1, T-3, T-5, T-13, IT-1, IT-2, IT-4x2, IT-5, EC-1, EC-6) from exit 2 to exit 0, plus 11 JSON block assertions. Total: 41 assertions, 0 failures.

### Verification: 1.3 [VERIFY] Quality checkpoint: syntax validation
- Status: PASS
- Command: `bash -n ralph-parallel/hooks/scripts/dispatch-coordinator.sh && bash -n ralph-parallel/hooks/scripts/session-setup.sh`
- Result: Both scripts pass bash syntax validation (exit 0)
- dispatch-coordinator.sh: 244 lines, syntax OK
- session-setup.sh: 152 lines, syntax OK
- No fixes needed, no commit required

### Verification: 1.5 [VERIFY] POC Checkpoint: run test suite end-to-end
- Status: PASS
- Command: `bash ralph-parallel/scripts/test_stop_hook.sh`
- Result: 12 tests, 48 assertions, 0 failures
- Tests:
  - T-SH1 PASS: Clean block-and-release cycle
  - T-SH2 PASS: Re-block with safety valve
  - T-SH3 PASS: Block counter reset on new dispatch
  - T-SH4 PASS: JSON output validity
  - T-SH5 PASS: Heartbeat write on block
  - T-SH6 PASS: Heartbeat-gated reclaim
  - T-PORT1 PASS: Matching session blocks with JSON (was exit 2)
  - T-PORT3 PASS: Legacy no coord blocks with JSON (was exit 2)
  - T-PORT5 PASS: Empty session_id blocks with JSON (was exit 2)
  - T-PORT13 PASS: stop_hook_active re-blocks (was immediate allow)
  - T-BC1 PASS: No lastHeartbeat -> reclaims normally (backward compat)
  - T-BC2 PASS: No coordinatorSessionId -> same behavior (backward compat)
- Verify command: `bash ralph-parallel/scripts/test_stop_hook.sh 2>&1 | tail -1 | grep -q '0 failed' && echo "PASS: all tests pass"` -> PASS
- Session isolation tests (test_session_isolation.sh): 20 passed, 10 failed (expected -- all failures are exit code 2 vs 0 changes from JSON migration). SessionStart hook tests all pass (no regressions). Failing tests noted for Phase 2 migration.
- Manual JSON verification: valid JSON output `{"decision":"block","reason":"[Dispatch: test-spec] 0/1 groups done..."}`
